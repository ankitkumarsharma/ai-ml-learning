{
  "name": "angular-node-realtime-chat",
  "version": "1.0.0",
  "private": true,
  "description": "Realtime chat application built with Angular, Node.js, Express, and Socket.IO.",
  "scripts": {
    "install:all": "npm install --workspaces",
    "dev": "concurrently \"npm run dev --workspace server\" \"npm start --workspace client\"",
    "server": "npm run dev --workspace server",
    "client": "npm start --workspace client",
    "build": "npm run build --workspace client"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
